from enum import auto, Enum

class Format(Enum):
    DEFAULT = auto()
    HEX = auto()
    DECIMAL = auto()
    DECIMAL_SIGNED = auto()
    BINARY = auto()
    PICTURE_BINARY = auto()
    CHAR = auto()
    STRING = auto()
